# Candidates (BankLedger)

No new surface: v1.62.0 adds no member, descriptor field, row field or major.

Delivered on the copy alone (class A, not offered):

- Kit revision 30: an empty watch-list table in `RESULTS.md` prints `None.` (ATS-20).
- Kit revision 31: files `Kit.layoutCap.exempt` covers leave `RESULTS.md`'s band table (ATS-21).
