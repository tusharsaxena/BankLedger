# 05 — Summary: LibKa0s v1.55.0 -> v1.56.0

Plan item RV-BL, 2026-09-24, branch `feat/2026-09-23-review-audit-remediation`. Written by hand from
the WA-01-amended local `revendor-libka0s.md`. Steps 0 and 2 to 4 only. Nothing pushed, and the
addon version is not bumped.

## The tag, and the per-file minors

`v1.55.0` -> `v1.56.0` (tag object `4622018`, commit `514fc0a`). The base comes from the `CLAUDE.md`
provenance line (v1.55.0), and the payload matched that tag byte for byte before the copy. Fifteen
files move a minor: Core 8, Lifecycle 2, Bus 2, Schema 2, Item 2, Media 4, Widgets 10, DebugLog 13,
Slash 15, Launcher 2, Options 24, OptionsWidgets 31, OptionsTabs 4, OptionsScroll 4 and Perf 13. No
file is added or removed, and there is no skew. The kit revision goes from 25 to 26 (three new files:
`asserts.lua`, `mock_events.lua`, `prose_lists.lua`). The table and the diffs are in `01_DELTA.md`,
sections 3c to 3f.

No span bundle is written and no base correction is recorded. Step 0 is ok. The 26 unrecorded tags
3h lists are BL-23's.

## Delivered on the re-vendor, with nothing asked for

- The launcher's missing-library notices print once per instance, without the `[LibKa0s] ` prefix,
  so the host printer's `[BL]` is the only tag (Launcher 2).
- `Item.QualityFromLink` reads the 11.1.5+ `|cnIQ<n>` link color (Item 2).
- Banner and header pages stop leaking a widget and a texture per render (OptionsTabs 4).
- `CreateOptionsPanel` parks in combat and replays at `PLAYER_REGEN_ENABLED` (Options 24).
- The debug console's buffer trims in batches at the cap, not once per line (DebugLog 13).
- `Core.Format` survives a secret value in a numeric slot (Core 8). `RegisterLSM` counts only what
  LSM holds (Media 4). The Bus tracking wrappers survive a newer AceEvent re-embed (Bus 2).
- Kit 26's stricter fakes and gates: frames start shown, the AceDB fake raises on a bad profile name,
  a recording `EventRegistry`, `__badEvents`, the lone-CR gate, the store-root prose files, and case
  names carrying the section sign.

## Contract blockers

None (`01_DELTA.md` 3g).

## Adopted, declined

Nothing adopted or declined here. The opt-ins are taken or left by this addon's M3 items: Core's
`SafeRegisterEvent` family (BL-06), the launcher's `isEnabled` / `disabledLine` gate and the
once-only notice pin (BL-08), Slash minor 15's refusal echo (BL-09) and Schema's `writeThrough`
(BL-10).

## Gates at the RV commit

| Suite | Result |
|---|---|
| `ka0s-bounded luacheck .` | 0 warnings / 0 errors in 71 files |
| `ka0s-bounded lua5.1 tests/run.lua` | 1019 passed, 1 failed, 0 skipped, 1020 total |
| `ka0s-bounded lizard` (libs and kit excluded) | no function above CCN 15 |
| `diff -r` of both payloads against the tag | empty |

The one red is caused by the stricter library: `Launcher: a host with neither broker library reports
it and does NOT raise` (`tests/test_launcher.lua:115`). Launcher minor 2 prints the missing-broker
notice once per instance, and the boot-time `Register` in `OnInitialize` has already spent it, so
the case's second `Register` prints nothing. BL-08 re-pins it. `docs/test-cases.md` and the README
test badge are regenerated by a later item (BL-DOCS at the latest), never by this commit.

## Open

- BL-23's tag list (25) disagrees with the 3h listing (26: adds v1.16.0, v1.17.0, v1.43.0; drops
  v1.24.0 and v1.29.0, already recorded). BL-23 should re-derive it.
