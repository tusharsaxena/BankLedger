# Pending-decision ledger — Ka0s Bank Ledger

The record of what `/wow-addon:pending-audit` has already asked you about, and what you decided.

The command sweeps this repo for everything still hanging — code markers, unexecuted audit and
review plan steps, doc open questions, open GitHub issues, recorded-but-unacted memory — and
interviews you one item at a time. This file is what stops it asking twice. Each row is matched on
its **ID plus its evidence hash** (the first 8 characters of `sha1` over the verbatim evidence
text), so a settled item stays settled, while an item whose underlying evidence has since *changed*
correctly comes back for a fresh decision.

Do not hand-edit the decision column to silence an item. If something needs re-opening, change the
thing itself; the hash will move and the item will surface on its own.

## Notation

| Marker | Value | Meaning | Re-surfaces? |
|---|---|---|---|
| 🟢 | `done` | Implemented this run | No — closed |
| 🔵 | `wont-do` | Decided it will never be done | No — closed |
| 🟡 | `deferred` | Not now; still on the books | Yes, as a collapsed count |

There is deliberately no red: nothing in this file is an error state. Green is resolved, blue is a
settled and deliberate close, and yellow is the only row type still asking for attention — so a
column of yellow is the file telling you what is left.

## Decisions

| ID | Evidence hash | Source | Decision | Date | Rationale |
|---|---|---|---|---|---|
| PLAN-01 | `63dc637d` | `docs/reviews/2026-07-27/05_FINAL_SUMMARY.md` | 🟢 done | 2026-07-31 | "This is done" — the in-client smoke tests were run and passed before v1.0.0 shipped; the bundle had simply never been filled in. Sign-off table completed and the status line corrected. |
| DOC-01 | `a7d0808f` | `docs/audits/2026-07-27/02_DEVIATIONS.md:16` (BL-04) | 🟡 deferred | 2026-07-31 | Not now. The rationale is already written in `locales/enUS.lua:8-12`; only the ARCHITECTURE ▸ *Documented deviations* record is missing, so the next standards-audit will re-raise BL-04 until it lands. |
| PLAN-02 | `a3a7777e` | `docs/audits/2026-07-27/05_EXECUTION_PLAN.md` Sprint 2 (BL-08) | 🟢 done | 2026-07-31 | Added to `docs/testing.md` as a new *Release checklist* section, so the re-drop rule sits where a release is actually run from. |
| DOC-02 | `39e646d7` | `docs/ARCHITECTURE.md` ▸ Known limitations | 🔵 wont-do | 2026-07-31 | Currencies are out of scope permanently — "remove references from existing docs and code". The reserved `Kind.CURRENCY` member, its label, the ARCHITECTURE field note and the README FAQ answer were all removed or rewritten so nothing implies the feature is coming. |
| DOC-03 | `9ca683ce` | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | 2026-07-31 | "Defer, and create a GitHub issue" — tracked as [#1](https://github.com/tusharsaxena/BankLedger/issues/1). A store-vs-store pairing rule needs its own design pass; the bags-must-move invariant is what keeps phantom rows out. |
| DOC-04 | `c079f94b` | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟡 deferred | 2026-07-31 | Deferred with a tracking issue, [#2](https://github.com/tusharsaxena/BankLedger/issues/2). Backfilling names means rewriting stored rows on the saved-variable write path, which wants a design pass of its own. |
| DOC-05 | `a39ce651` | `docs/ARCHITECTURE.md` ▸ Known limitations | 🟢 done | 2026-07-31 | The entry disowned itself ("a fallback rather than a live limitation"). Folded into the *Logo art* section's "the `.tga` must actually exist" rule, where the silent-failure behaviour is genuinely useful, and dropped from Known limitations. |
| DOC-06 | `ed4c3c2b` | `docs/superpowers/plans/` + `docs/superpowers/specs/` | 🟢 done | 2026-07-31 | Both artifacts described shipped work as pending. Stamped with a status line naming the delivering commits; the 21 step checkboxes were left unticked deliberately, since the header now removes the ambiguity. |

## LibKa0s adoption

One row per module, written when the call was made rather than at the end. `../LibKa0s/docs/adoption-prompt.md`
is the brief; `../LibKa0s/docs/adoption-report.md` is the audit these rows answer to. A **declined**
part of the library is recorded here on purpose — an unrecorded decision is indistinguishable from
an oversight, and the next consistency sweep will "fix" it.

| ID | Evidence hash | Source | Decision | Date | Rationale |
|---|---|---|---|---|---|
| LIBKA0S-01 | `48867ab5` | `../LibKa0s/docs/adoption-prompt.md` step 2 (vendor the test kit) | 🟢 done | 2026-08-01 | The kit is vendored to `tests/_kit/` and `tests/run.lua` / `tests/wow_mock.lua` are rebuilt on it; `tests/loader.lua` is deleted in favour of `_kit/loader.lua`. Taken FIRST, before any library module, because the kit's `mock_base.lua` is the only source of a `LibStub` with a real `NewLibrary` — without it no LibKa0s major can register headlessly and no seam is testable at all. The swap is behaviour-neutral: all sixteen pre-existing suites kept their exact case counts (603), and the seven new cases are `tests/test_harness.lua`. |
| LIBKA0S-02 | `1a7f0b3e` | `tests/_kit/framework.lua:79-96` (a listed suite with no file is SKIPPED) | 🟢 done | 2026-08-01 | Accepted, with a guard rather than a fork. The old runner raised on a missing suite; the kit skips it, which turns a typo into a green run with fewer cases. `tests/test_harness.lua` asserts the suite list and `tests/test_*.lua` agree in **both** directions, and both assertions were mutation-verified. Editing `tests/_kit/` to restore the raise was never an option. |
| LIBKA0S-03 | `6c2d94af` | `tests/_kit/mock_base.lua` vs the pre-adoption `tests/wow_mock.lua` | 🟢 done | 2026-08-01 | Ten base behaviours are deliberately overridden in the extender rather than adopted, each named in that file's header with the suite that needs it — the geometry-modelling frame stub, frames starting shown, OnHide firing, counting/cancelling timers, the raising `RegisterEvent`, the fixed-profile AceDB, the one-table settings-canvas registry, the plain-table chat sink, the inert AceGUI, and leaving `GameTooltip`/`StaticPopup_Show` nil. None is a kit gap: `mock_base.lua`'s own header states that single-consumer fidelity belongs in the consumer's extender. The `GameTooltip` / `StaticPopup_Show` pair is the one to revisit — taking the base's definitions flips eleven guarded production branches to their in-game path, which is more faithful but is a test-semantics change that does not belong in a harness swap. |
| LIBKA0S-04 | `2d1e57c9` | `../LibKa0s/docs/adoption-prompt.md` step 4 (Core) + `../LibKa0s/README.md` ▸ the printer descriptor | 🟢 done | 2026-08-01 | `LibKa0s-Core-1.0` adopted in `core/CoreSetup.lua`; the printer block leaves `core/Util.lua` (202 → 171 lines). `prefix` is passed as a **function** rather than as `NS.PREFIX`'s value — it reads the same today, but the printer is built once at load and the function form keeps a later change to the tag from being frozen out. No `sep` (the tag carries no trailing space of its own) and no `sink` (Core's default already resolves `DEFAULT_CHAT_FRAME` at call time, which is both what the old printer did and what the harness captures). One rendered-output change, pinned by a test: a zero-argument `NS.Print()` now emits the tag **and its separator** where the old single-concat form emitted a bare tag. Nothing in the addon calls it that way. The degraded fallback was rewritten to compose the same way, so a broken install renders identically to a working one. |
| LIBKA0S-05 | `9f4c31d2` | `../LibKa0s/README.md` ▸ `LibKa0s-Core-1.0` (`SKIN`, `ApplySkin`, `MakeCloseButton`) | 🔵 wont-do | 2026-08-01 | **Core's window chrome is DECLINED, deliberately.** `lib.SKIN` is a 12px `UI-Tooltip-Border` look against this addon's flat 1px `WHITE8X8` double border with a synthesised inner border, gold title tint and grey divider (`modules/Browser.lua:67-86`); `lib.MakeCloseButton` is 18×18 `GameFontNormalLarge` with a fixed red hover against this addon's 24×24 `STANDARD_TEXT_FONT`@24 with a **class-coloured** hover, shared by the ledger window, the console and the export popups. Adopting either is a visual redesign of every window, not an extraction. Only the printer and `SafeToString`/`IsConcatSafe` are taken. Recorded rather than left implicit: a declined convergence nobody wrote down is indistinguishable from an oversight, and the next consistency sweep would "fix" it. This decision is what makes `LibKa0s-DebugLog-1.0` non-trivial — see the DebugLog row. |
| ~~LIBKA0S-06~~ | `7b30d8e4` | `../LibKa0s/docs/adoption-prompt.md` "Standing hazards" (CRLF) + this repo's missing `.gitattributes` | 🟡 deferred | 2026-08-01 | **DONE — the deviation this row describes no longer exists, and the row is struck rather than deleted so the reasoning stays readable.** `9325663` added `.gitattributes` pinning `* text=auto eol=crlf` byte-identically to the library's, and `file -b` now reports CRLF throughout the working tree, so both halves of the text below are false as present-tense statements: the file exists and the tree is not LF. All four vendor diffs are empty on both readings, byte and content — this repo is clean *by construction* now rather than by luck. Caught by the 2026-08-01 v2 adoption report (recommendations §9b), which found a deferred row describing a state the working tree contradicted; every other superseded row here carries its pointer and this one had been missed. Original text follows. ~~**A standards deviation surfaced by the adoption, not caused by it, and not fixed here.**~~ The adoption prompt states that every repo in the collection pins `* text=auto eol=crlf` in `.gitattributes`; this repo has **no `.gitattributes` at all** and its working tree is pure LF. Consequence: the vendored `libs/LibKa0s/` and `tests/_kit/` are CRLF (copied verbatim from the library's tree) while everything else here is LF, and `docs/test-cases.md` is now CRLF too because the shared kit's `--list` renderer writes CRLF itself — deliberately, so the regeneration command needs no `sed` pipeline. The vendor gate currently reports empty on **both** halves, so nothing is wrong today. Adding the pin is a repo-wide working-tree change well outside this adoption and is the user's call. |
| LIBKA0S-07 | `4e8b21c7` | `../LibKa0s/docs/adoption-prompt.md` "When the library itself has to change" | 🟢 done | 2026-08-01 | **A library gap, fixed upstream rather than worked around here.** `LibKa0s-DebugLog-1.0` adopted in `core/DebugLogSetup.lua` (131 lines); `modules/DebugLog.lua` (357 lines) **deleted outright**. The blocker was LIBKA0S-05: the module hard-wired Core's chrome, and its only hook was `skin`, a TABLE reaching the three backdrop calls and nothing else — an inner-border child frame, a title tint and a divider tint are calls, not fields, and the close button had no hook at all. Two optional descriptor fields, `applySkin` and `makeCloseButton`, were added upstream at **DebugLog minor 4**, both defaulting to minor 3's behaviour; the title-bar offsets became derived from the close button's width in the same change, because minor 3's hard-coded `-30` was exactly the left edge of this addon's 24-wide × and the six-pixel gap would have vanished. Written test-first in `../LibKa0s` (nine cases, eight confirmed failing first), then re-vendored into AbsorbTracker, KickCD and ConsumableMaster with all three suites unchanged at 462 / 643 / 554 — which is what makes "additive" checkable rather than asserted. |
| LIBKA0S-08 | `5c9a76b1` | `../LibKa0s/README.md` ▸ `ConsoleCheckbox()` data contract | 🔵 wont-do | 2026-08-01 | **`D:ConsoleCheckbox()` is DECLINED.** Its label is byte-identical to this addon's (`Debug console`) but its tooltip is not, and adopting it would both rewrite a user-visible string nobody asked to change and bypass the `state.debugConsole` schema row that `settings/Schema.lua` marks `sessionOnly = true` — the row is what makes the checkbox visible to `/bl list`, `/bl get` and the panel's Defaults button. The schema row stays; the library surface goes unused. Recorded because an unused surface that looks like an oversight is exactly what the next sweep "fixes". |
| LIBKA0S-09 | `8d40e2f5` | `modules/DebugLog.lua:300-309` vs `../LibKa0s/LibKa0s/DebugLog.lua` `onVisibilityChanged` | 🟢 done | 2026-08-01 | **A bug fixed by the adoption, recorded so it is not mistaken for drift.** The old console called `syncPanel()` inline from `D:Show`/`D:Hide`, but the window is registered in `UISpecialFrames`, so **Esc hides it without going through `D:Hide`** — and the settings panel's `state.debugConsole` checkbox has gone stale on every Esc close since the console was written. The library fires the callback from the frame's own `OnShow`/`OnHide` instead, which catches Esc and the close button too. Cost: one extra callback during the frame's build-time `Hide`, which `P:Refresh` early-returns on because the panel is not shown. Pinned by a case that drives the frame directly, since a mock's `Hide()` is the only way to reach that path. |
| LIBKA0S-10 | `3f7c05d8` | `../LibKa0s/docs/adoption-prompt.md` convergence #1 (`reset` takes a path) | 🟢 done | 2026-08-01 | **Adopted — and it was already converged, which is not the same as "not applicable".** `/bl reset <path>` was path-scoped and `/bl resetall` already existed before the adoption, so there was no page-shaped form to remove, nothing to deprecate and no confirmation popup to re-anchor. `Sl:CliReset` now delegates to `Sl:CliReset` on the library instance unchanged in meaning. Asserted rather than assumed: a case proves a page NAME does not resolve as a reset target. |
| LIBKA0S-11 | `6a91be44` | `../LibKa0s/docs/adoption-prompt.md` convergence #2 (one command-row formatter) | 🟢 done | 2026-08-01 | **Adopted, and it is user-visible.** `settings/Panel.lua`'s landing page carried a SECOND formatter for `NS.COMMANDS` — `"|cffffff00/bl %s|r  |cffffffff—|r  %s"`, doubled spaces around an em dash explicitly wrapped white, description bare — while chat used a different one. It now renders `NS.Slash:LandingRows()`, the library's single row formatter. What a user sees change: the spacing tightens to one space either side of the dash, the dash loses its colour span, and the description gains one. Accepted: two renderers for one table in one repo drift the moment either is touched, and these two already had. |
| LIBKA0S-12 | `bd25e103` | `settings/Schema.lua` row types and option lists | 🟢 done | 2026-08-01 | The schema's vocabulary moved to the one the library reads: `type = "boolean"` → `"bool"` (6 rows), `options` → `values` (3 rows), and each option entry's `label` → `text` (10 entries in `core/Constants.lua`). Neither rename is cosmetic and both fail SILENTLY. Against `"boolean"` the library answers `unknown setting type 'boolean'` and all six become un-settable from chat — over half the settings surface. Against `options` the library's `enumList` sees nothing, so `/bl set settings.retentionDays 99` stores 99 and the panel has no label for what is stored. Both are now pinned by cases, and both mutations were verified red. |
| LIBKA0S-13 | `c47a9f16` | `settings/Slash.lua` rendered output vs `../LibKa0s/LibKa0s/Slash.lua` | 🟢 done | 2026-08-01 | **The rendered-output changes a user will notice, accepted knowingly.** (1) The help header gains an em dash: `v1.0.0 slash commands` → `v1.0.0 — slash commands`. (2) Every help row gains a two-space indent. (3) Gold/white colour codes flip from lowercase `\|cffffff00` to uppercase `\|cFFFFFF00` on every help row, every `/bl list` value row and every get/set/reset echo — the list header and its azure group headings stay lowercase, which is the library's own deliberate exception. (4) `all settings reset to defaults` gains a capital A. (5) A failed `set` becomes two lines: `Invalid value for <path>` then an indented reason, each carrying its own tag. (6) `/bl set <path>` with no value now reports what is wrong with the value instead of printing usage; a bare `/bl set` still prints usage. Two behaviour changes come with them: a number outside a slider's range now CLAMPS rather than storing what was typed, and `/bl set settings.excludedStores <anything>` now refuses instead of writing a raw string into a table-typed setting the capture gate reads as a set — a live bug the adoption fixes. |
| LIBKA0S-14 | `e820d4b7` | `settings/Slash.lua:146-147` (`LIST_GROUP_ORDER`) | 🔵 wont-do | 2026-08-01 | The hand-maintained `/bl list` group-order constant is **deleted rather than ported**. It is the F-007 defect itself: it named `"Window"`, a group that no longer existed, and omitted `"Master Controls"`, so every name in it was inert and the listing silently fell through to first-seen order — while a test that compared it against itself passed. The library has no equivalent and needs none: it groups strictly in `allRows()` declaration order, which IS the panel's order, so the two cannot disagree. The regression guard is rebuilt against the schema instead of against a second list, and there is no longer a second list to rot. |
| LIBKA0S-15 | `a1d9f30b` | `standards/options-ui.md` §11 and §41 (WowAddonStandards v2.15.0) | 🟢 done | 2026-08-01 | **A conformance defect, found by using the addon rather than by the adoption, and fixed here.** options-ui-§11 requires that "an open panel MUST reflect live state after a mutation (a checkbox write, **a slash `set`**, a profile switch, a list add/remove)", and §41 puts that refresh in the addon's **single write seam** — "the same function `/<slash> set` calls". `NS.Schema:Set` fired the row's `onChange` and stopped, and the only two callers of `P:Refresh` were the confirm-gated full reset and the debug console's visibility sync. So `/bl set`, `/bl reset` and `/bl resetall` never repainted an open settings window; the values were right, just late, because the panel's own `OnShow` re-read them on reopen. `P:Refresh` additionally walked `P.general` alone, so even once wired every other page would have stayed stale. Fixed at the seam, over a page **registry** rather than a named field, so "every page refreshes" is true by construction as pages are added. `P:Batch` coalesces a bulk write into one repaint — ten schema rows through the seam would otherwise be ten refreshes, and one of General's refreshers walks the whole ledger. Pre-existing: present at `445e19e`, before this branch. |
| LIBKA0S-16 | `f52b7c6e` | `standards/options-ui.md` §11/§41 vs `../LibKa0s/README.md` ▸ Options descriptor | 🔵 wont-do | 2026-08-01 | **No upstream change: neither the standard nor the library has a gap here.** The standard already states the rule twice and unambiguously (§11 names "a slash `set`" explicitly; §41 names the write seam as where the refresh belongs), so there is nothing to add to WowAddonStandards — BankLedger was simply not conforming. LibKa0s-Options-1.0 already ships the mechanism the standard describes — per-widget refresher closures, `RefreshScalars` for in-place re-sync, `RefreshAllPanels` for structural, each refresher `pcall`'d — and its Slash descriptor documents `set` as "Route it through the host's single write seam, so a panel change takes the same path a slash command does — same debug line, same `onChange`, same refresh." The library says the right thing; this host was not doing it. Recorded because "we checked upstream and it was already correct" is worth exactly as much to the next reader as a change would have been. |
| LIBKA0S-17 | `7e3a15d0` | `../LibKa0s/LibKa0s/Perf.lua:634-665` vs `modules/Ledger.lua:483-486` | 🔵 wont-do | 2026-08-01 | **`LibKa0s-Perf-1.0` DECLINED, and the reason is structural rather than a judgement about thresholds.** The instrument measures inside **combat-gated** windows: `onUpdate` opens a window only when `UnitAffectingCombat("player")` is true (`Perf.lua:655-665`), and `P.on` — the flag every hot-path bracket reads — is set true only in `openWindow` (:634) and false in `closeWindow` (:644). Bank Ledger's entire capture engine is gated on `NS.State.openContext`, set only by a bank or guild-bank frame opening: an **out-of-combat NPC interaction**. The two conditions never overlap, so every declared bucket would read 0 calls / 0.000 ms — not "rarely", but always, by construction. That is worse than the "reads 0.000 forever and adds a row that says nothing" case the adoption prompt warns about, because here it is guaranteed. The FPS arms fare no better: the only code reachable during a combat window is the early return `if not NS.State.openContext then return end` (`modules/Ledger.lua:620`), and there is no `OnUpdate` anywhere in the addon — already asserted at `docs/reviews/2026-07-27/03_SMOKE_TESTS.md:414`. This addon's real costs are bursty and out-of-combat: Reconcile's ~1600-slot rescan (`modules/Ledger.lua:519, 532-536`) and `Database:Stats`' O(n) pass plus ~20 sorts (`core/Database.lua:156, 325-398`), both already debounced and both already measured with `GetAddOnCPUUsage` and `collectgarbage("count")` deltas by the 2026-07-27 review. Nothing is saved by declining — `Perf.lua` and `PerfPanel.lua` are vendored and loaded regardless (`libs/LibKa0s/LibKa0s.xml:8-9`). What adoption would cost is a second SavedVariables global, a `debugprofilestop` lint entry, two new Ledger entry points (`L:Enable` is not re-entrant, `modules/Ledger.lua:761`), a carve-out in the SessionChanged sole-sender invariant (`:475`), an `SW:Enabled()` edit, a `perf` verb, README/COMMANDS parity and tests — all to produce a report of zeros. **Revisit only if** the library grows a non-combat window mode (an arm bounded by a bank session), or this addon acquires a per-frame or combat-log path it does not have today. |
| LIBKA0S-18 | `b6f2c84a` | `../LibKa0s/LibKa0s/OptionsWidgets.lua:466` vs `Slash.lua:198-214` | 🟢 done | 2026-08-01 | **A library gap found ahead of the Options adoption and fixed upstream: two majors disagreed about what one schema row IS.** `LibKa0s-Slash-1.0` has treated `type = "number"` carrying a `values` list as a constrained enum since `-1.0` — and this addon ships a green case asserting it (`tests/test_libka0s.lua`, "a numeric dropdown now REFUSES a value outside its list"). `O.RenderField` meanwhile sent every number row to `makeSlider` without consulting `values`. `settings.qualityThreshold` and `settings.retentionDays` declare no `min`/`max`/`step`, so under the library they would have rendered as unusable 0-to-1 sliders. Fixed at **OptionsWidgets minor 5** by inferring from `values` (not by an opt-in, which would have left the two majors still disagreeing for any row declaring `values` and nothing else). Additive: these two rows are the only `number`+`values` rows in the collection, so AbsorbTracker, KickCD and ConsumableMaster stayed at 462 / 643 / 554. |
| ~~LIBKA0S-19~~ | `d4c1907f` | `../LibKa0s/LibKa0s/Options.lua` (no `OnCommit`/`OnDefault`/`OnRefresh`) vs `standards/options-ui.md` §1 | 🟡 deferred | 2026-08-01 | ~~**Raised, not acted on.**~~ **SUPERSEDED by LIBKA0S-24 — done, upstream, in all three places.** Row preserved rather than deleted: the analysis is why the fix took the shape it did. Blizzard's Settings window calls `OnCommit`, `OnDefault` and `OnRefresh` on a registered canvas frame. This addon stamps all three (`settings/Panel.lua`) and asserts them (`tests/test_panel.lua`, three cases). **LibKa0s never sets any of them** — `grep -rn "OnCommit\|OnDefault\|OnRefresh" LibKa0s/` is empty — and **neither do AbsorbTracker, KickCD or ConsumableMaster**, so all three shipped addons are missing the contract. But `standards/options-ui.md` §1 does not require it either: the trio appears nowhere in the standard, even though this repo's own comment cites "options-ui-§1" for it. So this addon is a superset of what the standard asks, three others are a subset of what Blizzard expects, and the standard is silent. Per `CLAUDE.md` that is a deviation to surface rather than a fix to apply: either the standard should state the requirement (and the library should stamp the inert pair and accept an `onDefault`), or this addon's belief that §1 requires it is wrong and the comment should be corrected. **Not a blocker for the Options adoption** — the host keeps stamping them either way. |
| ~~LIBKA0S-20~~ | `0b58e9c3` | `../LibKa0s/docs/adoption-prompt.md` step 4 (Options) | 🟡 deferred | 2026-08-01 | ~~**`LibKa0s-Options-1.0` prepared but NOT adopted.**~~ **SUPERSEDED by LIBKA0S-21 — adopted.** Row preserved rather than deleted so the reasoning for the pause stays readable. Everything blocking it has landed: the numeric-enum dispatch upstream (LIBKA0S-18), the row vocabulary (`step`, `skipRender`, `solo`/`skipRender` naming), the `options`→`values` and `label`→`text` renames from the Slash milestone, and confirmation that swapping the test mock's inert AceGUI for the kit's real widget factory changes **zero** currently-passing cases (the addon never calls `AceGUI:Create` during the suite). What remains is the `settings/Panel.lua` reduction itself: ~385 of its 890 lines have a library equivalent, ~505 survive, and ~25 call sites must be rewired to the instance. Stopping here rather than starting it was deliberate — a half-migrated 890-line panel is worse than an un-migrated one, and every other module in this adoption was landed at a green, committed boundary. The full line-range inventory (DELETE / KEEP / REWIRE), the four decisions it needs (`O.RestoreAllDefaults` and `O.InlineButtonPair` are knowing declines; the combat-refusal grey moves `\|cff808080`→`\|cffaaaaaa`; the MultiCheck becomes a bespoke cell inside the library's flow rather than a page-level opt-out) and the `settings/OptionsSetup.lua` descriptor shape are in this session's recon and should be re-derived rather than trusted if the source has moved. |
| LIBKA0S-21 | `c93f1a07` | `../LibKa0s/docs/adoption-prompt.md` step 4 (Options) | 🟢 done | 2026-08-01 | **`LibKa0s-Options-1.0` adopted.** `settings/OptionsSetup.lua` (169 lines) holds the descriptor and `NS.Helpers`, which **IS** the library instance rather than a wrapper (options-ui-§1). `settings/Panel.lua` 890 → 630: the canvas shell, the header and breadcrumb, the always-shown scrollbar patch, the lazy Defaults button, the tooltip helper, the three widget makers and the two-column flow engine all deleted — fourteen top-level blocks, 301 lines — and the three pages now declare themselves through `RegisterOptionsPage` + `SetRenderer`, so the library owns the render timing. Two behaviour gains come free: the Blizzard AddOns **sidebar** route now refuses to render under combat (this addon guarded only `/bl config`), and a hidden page marks itself dirty and repaints once on its next show instead of the host tracking that by hand. |
| LIBKA0S-22 | `2b7e5d41` | `../LibKa0s/LibKa0s/Options.lua` `RestoreAllDefaults` / `InlineButtonPair` / `runRefreshers` | 🔵 wont-do | 2026-08-01 | **Three library surfaces knowingly DECLINED.** (1) `O.RestoreAllDefaults` — this addon's global reset must also clear the filter lists and the saved ledger view, neither of which is a schema row, and it is already one implementation shared with `/bl resetall`; routing the panel through the library's version would fork them. (2) `O.InlineButtonPair` — it lays out two *action buttons* in their own row; here the partner is a schema **Slider** ("Reset all" beside Window scale), which is the library's `pairWith` instead, and the host keeps a one-button `makePairButton` reading `O.BUTTON_PAIR_REL`. (3) The library's `runRefreshers` `pcall`s each refresher but does **not** report the failure, where this addon's deleted `safeRun` logged it through `NS.Debug` — a small, real loss of diagnosability accepted rather than forked, since the alternative is a host copy of the library's own refresh loop (anti-pattern #47). |
| LIBKA0S-23 | `8f04c6b2` | `settings/Panel.lua` `makeMultiCheck`; `tests/test_panel.lua` canvas-contract cases | 🟢 done | 2026-08-01 | **Two real bugs the adoption's new render coverage caught immediately, neither of which any prior test could see.** (1) The store grid **registered** a refresher but never **ran** it, so it drew every box unticked regardless of what was stored and only corrected itself on the next unrelated write. It worked before only because the old OnShow handler ended in a `P:Refresh()`; the library's renderer does not, because a maker owns seeding its own widget. (2) The three canvas-contract cases were **vacuous**: the mock's frame stub synthesises a no-op for any PascalCase key, so `type(p.OnCommit) == "function"` was true whether or not the addon ever set it. Rewritten with `rawget`, which immediately exposed a third bug — the **landing page** was never stamped at all, and the sub-pages were stamped inside their renderers, which run on FIRST SHOW when Blizzard can call `OnCommit` on a page the user has never opened. Now stamped over the library's registry right after `CreateOptionsPanel`. |
| LIBKA0S-24 | `5aeb2f18` | `standards/options-ui.md` §1 + `../LibKa0s/LibKa0s/Options.lua` `CreatePanel` | 🟢 done | 2026-08-01 | **Closes LIBKA0S-19, upstream in all three places rather than locally.** The standard now REQUIRES the trio on every frame handed to `RegisterCanvasLayout(Sub)category` and names who supplies it; `LibKa0s-Options-1.0`'s `CreatePanel` stamps it at **Options minor 5**; and this addon deleted its own `stampCanvasContract` and the post-`CreateOptionsPanel` loop, because a host on the library now MUST NOT set them itself. `setDefaultsAction` keeps parking `defaultsOnClick` and no longer sets `OnDefault`: the library's is a **forwarder** to it, so the footer control and the header button cannot drift by construction. Re-vendored into AbsorbTracker, KickCD and ConsumableMaster — all three unchanged at 462 / 643 / 554, which proves it additive at the API level, and all three **gain a working Settings-window footer Defaults control** they never had, which is a user-visible change rather than a silent repair. |
| LIBKA0S-25 | `9c07be44` | `tests/test_panel.lua` "OnDefault is the same closure as the header Defaults button" | 🟢 done | 2026-08-01 | **An assertion changed to accommodate a change I made, recorded because that is the move that deserves saying out loud.** The case compared `OnDefault` and `defaultsOnClick` as the SAME function object, which held while this addon set both from one closure. The library's `OnDefault` is a forwarder, so they are deliberately no longer identical — and identity was only ever a proxy for the thing that matters. It now asserts BEHAVIOUR: swap in a counting closure, call `OnDefault`, assert it ran. Mutation-verified against the wrong implementation (`panel.OnDefault = panel.defaultsOnClick`, which captures nil because hosts park the handler after `CreatePanel` returns) — the new form catches it, the old form did not exist to. |
| LIBKA0S-26 | `options-tripwire` | LibKa0s `docs/adoption/2026-08-01-v2/04_RECOMMENDATIONS.md` §6b | 🟢 done | 2026-08-01 | The Options half of the `L`-trap tripwire, alongside the Core one this repo already carried. It is deliberately NOT the Core case copied across: `Options.lua` ships its own `lib.STRINGS` table, so `rawget(lib, "STRINGS") == nil` would fail there against a module behaving exactly as designed. What transfers is the source half — Options resolves its strings from `lib.STRINGS` with no descriptor override path anywhere in the three files, so there is nothing a key-echoing locale table could shadow. The case sweeps `Options.lua`, `OptionsWidgets.lua` and `OptionsScroll.lua` for a `d.L` read and pins `lib.LAYOUT` alongside `lib.STRINGS`, because `local L = lib.LAYOUT` at the top of that file is geometry and the distinction is what a rename would lose. Mutation-verified. The library-side prompt was corrected in the same pass: it had claimed three consumers carried an Options substitute, which was true of one. |
| LIBKA0S-27 | `6e0181ed` | `libs/LibKa0s/Core.lua` (Core minor 3), WowAddonStandards v2.16.0 | 🟢 done | 2026-08-02 | **The Ka0s window edge became the library's, and it is this addon's edge.** An in-game pass put all five adopters' debug consoles on screen at once and they split into two looks: this addon and LootHistory drawing a flat 1px black edge with a 1px grey inner highlight, a gold title and a grey divider; AbsorbTracker / ConsumableMaster / KickCD on the library's 12px `UI-Tooltip-Border` with a black divider. Two of the five had overridden the shared seam in order to look right, which is the shared seam failing at the thing it exists for. Fixed **upstream**: `standalone-windows-§2` now specifies the edge normatively and LibKa0s v1.3.0 makes `Core.SKIN` carry it. Nothing here changed appearance; the three siblings did. `applySkin` stays — its purpose narrows from "rescue the console from a default that does not match" to "keep the console tracking `modules/Browser.lua`'s own re-skin seam". |
| LIBKA0S-28 | `1dd31fcc` | `core/DebugLogSetup.lua`, `libs/LibKa0s/DebugLog.lua` (minor 6) | 🟢 done | 2026-08-02 | **The `makeCloseButton` hook is dropped: the console and the copy window wear Core's ×, not this addon's.** Same root cause as LIBKA0S-27, one descriptor field along. The hook was taken on the reasoning that all of this addon's windows should close alike — which produced two diagnostic windows matching this addon and no other, while the three siblings that passed nothing wore Core's thin 18Ý18 ×. The split the standard now draws (`standalone-windows-§2`): the window **edge** is shared across every Ka0s window, but the **close control on a library-drawn window is the library's**. The Bank Ledger browser keeps its 24Ý24 class-coloured glyph — that window is this addon's and was never in question. Upstream too: LibKa0s v1.3.1 documents the field as rarely-right and now-unused, with a regression guard that a console built without it reaches `Core.MakeCloseButton` exactly twice. |
